{
  "name": "ts-project-template",
  "version": "1.0.0",
  "description": "Eine Vorlage für TypeScript-Projekte.",
  "main": "./build/index.js",
  "private": true,
  "scripts": {
    "clean": "rimraf ./build",
    "typecheck": "tsc",
    "transpile": "babel --extensions \".ts\" -d ./build ./src",
    "build": "npm run clean && npm run typecheck && npm run transpile"
  },
  "author": "Wlad G. Gumenyuk",
  "license": "MIT",
  "devDependencies": {
    "@babel/cli": "^7.20.7",
    "@babel/preset-env": "^7.20.2",
    "@babel/preset-typescript": "^7.18.6",
    "rimraf": "^3.0.2",
    "typescript": "^4.9.4"
  }
}
